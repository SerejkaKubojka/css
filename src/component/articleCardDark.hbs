<div class="articles_card" id="articl-{{id}}">
    <div class="articles_card_image" style="background-image: url({{photo}});"></div>
    <h2 class="articles_card_title">{{name}}</h2>
    <p class="articles_card_text">{{description}}</p>
    <a href="{{linkUrl}}" class="button articles_card_link">Read all</a>
</div>